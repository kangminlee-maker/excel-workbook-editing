# Google Sheets Projects

Use one folder per `spreadsheetId + gid` pair.

The spreadsheet id identifies the Google Sheets document. The `gid` identifies
the sheet/tab surface being analyzed. If the work later becomes whole-workbook
level, add a workbook-level project file beside the `gid-*` folders rather than
mixing tab-specific and workbook-level claims silently.
