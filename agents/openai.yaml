interface:
  display_name: "Excel Workbook Editing"
  short_description: "Workbook editing, reconciliation, structure inspection, and Excel-engine validation"
  default_prompt: "Use $excel-workbook-editing to edit or reconcile an Excel workbook with the model-agnostic agent workflow: inspect workbook structure before flattening it, check source differences and logic differences before chasing output gaps, preserve source-of-truth alignment, validate formula results with the real Excel engine, and scan for formula errors."
